
name: ci

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-latest
    environment:
      name: dev
    steps:
      -
        name: Login to ACR
        uses: docker/login-action@v2
        with:
          registry: bsrdevacr01.azurecr.io
          username: ${{ vars.ACR_SP_APP_ID }}
          password: ${{ vars.ACR_SP_PASSWORD }}

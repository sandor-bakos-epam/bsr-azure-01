name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: windows-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Run Azure PowerShell script
        uses: azure/powershell@v1
        with:
          inlineScript: |
            if(Test-Path -Path ${{ github.workspace }}\data\xyz)
            {
              foreach($file in Get-ChildItem -Path ${{ github.workspace }}\data\*.json -Name)
              {
                $pipeline = Get-Content -Raw ${{ github.workspace }}\data\$file | ConvertFrom-Json
                echo $pipeline.name
              }
            }
            else { "Path doesn't exists!" }
          azPSVersion: "latest"
      - name: Run iteration with powershell
        run: |
          foreach($file in Get-ChildItem -Path ${{ github.workspace }}\data\*.json -Name)
          {
            $pipeline = Get-Content -Raw ${{ github.workspace }}\data\$file | ConvertFrom-Json
            echo $pipeline.name
          }